   kernel void simple (                       global const float* a,                 global const float* b,                 global float* c                    )                                      {                                          int i = get_global_id(0);              float tmp = 0;                         for(int j = 0; j < 100000; ++j)            tmp += a[i] + b[i];                c[i] = tmp;                        }                                   